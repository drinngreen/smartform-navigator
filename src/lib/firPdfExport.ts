// FIR PDF Export - stub
export async function generateFIRPdf(formData: any): Promise<Blob> {
  // Placeholder - real implementation uses pdfkit
  return new Blob(["PDF placeholder"], { type: "application/pdf" });
}
